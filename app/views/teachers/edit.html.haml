.col-lg-4{class: "new-teacher"}
  .panel-title
    %h4 New Teacher
  = simple_form_for(@teacher) do |f|
    = f.error_notification

    .form-inputs
      = f.input :name
      = f.input :degree
      = f.file_field :tavatar
      = f.file_field :tcover

    .form-actions
      = f.button :submit
  = link_to 'Back', dashboard_path